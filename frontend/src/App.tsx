import { FC } from "react";

import "./styles/main.scss";
import MainPage from "./MainPage";

const App: FC = () => {
  return (
    <div className="center">
      <MainPage />
    </div>
  );
};

export default App;
